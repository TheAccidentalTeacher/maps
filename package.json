{
  "name": "mrsomersmaps-netlify-functions",
  "version": "1.0.0",
  "description": "Serverless functions for Mrs. Somers Maps API proxy",
  "private": true,
  "dependencies": {
    "node-fetch": "^2.6.7",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "scripts": {
    "dev": "node local-dev-server.js",
    "dev:netlify": "netlify dev",
    "build": "echo 'No build needed for static site'",
    "deploy": "netlify deploy --prod"
  }
}
